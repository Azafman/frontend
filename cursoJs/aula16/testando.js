function vamos(){
    var num1 = Number(prompt('Digite um número: '))
    var num2 = Number(prompt('Digite um número: '))
    confirm('A soma dos dois números é ' + soma(num1,num2))
    function soma(n,nn){
        return n+nn
    }
}

